using UnityEngine;

public class Button43AScript : MonoBehaviour
{
    public DialogueManager DM;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    void OnMouseDown()
    {
        DM.MakeChoice43A();

    }
}
