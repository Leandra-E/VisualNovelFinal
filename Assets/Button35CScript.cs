using UnityEngine;

public class Button35CScript : MonoBehaviour
{
    public DialogueManager DM;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    void OnMouseDown()
    {
        DM.MakeChoice35C();

    }
    
}
